using System.Windows;
using System.Windows.Controls;

using StudentManager.UI.Views;

namespace StudentManager.UI;

public partial class MainWindow : Window {
  public MainWindow() {
    InitializeComponent();
  }
}
